# 2026-02-26

## Telegram Groupes — problème non résolu
- Bot @OpenClawBidi_bot fonctionne en DM mais **pas dans les groupes Telegram**
- Groupe "Alexis et OpenClaw" — 2 IDs identifiés : `-5205385099` (groupe normal) et `-1003724334036` (supergroupe migré)
- Config ajoutée : `channels.telegram.groupPolicy: "open"` + `channels.telegram.groups` avec les deux IDs
- `agents.list` configuré avec `groupChat.mentionPatterns: ["@OpenClawBidi_bot", "openclaw", "bidi"]`
- Le curl `getUpdates` montre que Telegram envoie bien les messages du groupe au bot
- Mais le gateway OpenClaw ne les traite pas — aucune trace dans les logs
- Suspicion : problème d'offset Telegram ou bug OpenClaw avec les groupes
- **À investiguer** : lancer le gateway en mode `--verbose` pour voir les updates reçues

## Token gateway mismatch — récurrent
- Le `gateway.auth.token` et `gateway.remote.token` se désynchronisent fréquemment
- Fixé manuellement via python3 pour aligner les deux dans openclaw.json
- `openclaw gateway install --force` + `stop/start` (pas `restart`) est la procédure qui marche
- Le wizard `openclaw configure --section model` peut écraser des settings existants

## Sandbox désactivé
- `agents.defaults.sandbox.mode` passé de `"all"` à `"off"`
- Permet l'accès direct à la machine d'Alexis depuis l'agent

## Outils installés
- **summarize** v0.11.1 (`npm i -g @steipete/summarize`) — CLI pour résumer URLs, PDFs, vidéos
- **scrapling** v0.4 (`pipx install "scrapling[all]"`) — Web scraping adaptatif, bypass anti-bot, serveur MCP
  - Playwright browsers installés via `scrapling install`
  - MCP server configuré dans mcporter : `scrapling mcp` (stdio)
- **Notion MCP** — `npx -y @notionhq/notion-mcp-server` configuré dans mcporter
  - Token : `ntn_Y88956521525zC350AAPbSgRT0XmxoPpz7inJOrztWUeDu`
  - Intégration "OpenClaw" créée dans Notion

## Notion — base "🚗 Suivi Enchères Véhicules"
- Database ID : `31321238-e138-818f-ba0a-e652f05d36bb`
- Page parent "test" : `31321238-e138-8099-ae1c-eebb90d39eb7`
- Colonnes : Véhicule, Estimation (€), Date vente, Maison de ventes, Lieu, Type (select), Statut (select), Lien (url), KM, Année, Notes
- 16 lots ajoutés (Tesla Model Y, Model 3, Volvo XC90)
- Sources : interencheres.com (ventes à venir) + auction.fr (lots similaires)
- Prix d'adjudication passés non accessibles gratuitement (abonnement auction.fr requis)

## Interencheres — ventes à surveiller
- **27/02** : Alcopa Marseille (4 Tesla + 1 XC90) + Grasse vente judiciaire (4 Tesla)
- **28/02** : Liquidation judiciaire Model Y à 10k€ (Maître Casal) + Ardèche Enchères
- **02-06/03** : Mercier Auto, PAM Enchères, Balsan Enchères, Dieppe

## Whisper API
- Skill `openai-whisper-api` prêt (ready) dans OpenClaw
- Clé OpenAI configurée via `openclaw configure --section model` (OAuth Codex)
- À tester : envoyer un vocal au bot en DM

## OpenClaw mis à jour
- Version passée de 2026.2.24 à 2026.2.25

## HEARTBEAT.md modifié
- Retiré la règle "18h-20h30 : temps famille, ne pas déranger sauf urgence"
